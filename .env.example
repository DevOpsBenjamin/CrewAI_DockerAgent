# code-server auth (choose one)
PASSWORD=change-me-long-random
# HASHED_PASSWORD=$argon2id$...  # optional pre-hash

# Reverse tunnel target (your VPS)
GATE_HOST=jetdail.fr
GATE_USER=root
# For one-time bootstrap only; leave empty otherwise
GATE_PASSWORD=
# Remote bind
REMOTE_PORT=8080
LOCAL_PORT=8080
